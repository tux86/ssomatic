name: PR Title

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  pull-requests: read

jobs:
  lint:
    name: Validate PR title
    runs-on: ubuntu-latest
    steps:
      # Ensures the PR title is a valid Conventional Commit, since squash-merging
      # uses it as the commit message that release-please parses.
      #
      # `main` is allowed because release-please titles its own Release PR
      # `chore(main): release X.Y.Z`, which this check would otherwise reject.
      - uses: amannn/action-semantic-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            perf
            refactor
            docs
            style
            test
            build
            ci
            chore
            revert
          scopes: |
            main
            cli
            aws
            ci
            deps
          requireScope: false
